export interface BioItem {
    id: string
    name: string
    content: string
    imageUrl: string
  }
  
  export interface ArtworkItem {
    id: string
    title: string
    imageUrl: string
    category: string
    description: string
    dateCreated: string
  }